global:
  scrape_interval: 5s # Ask for data each x time

scrape_configs:
  - job_name: 'spring-boot-api'
    metrics_path: '/actuator/prometheus'
    static_configs:
      # - targets: ['finance-api:8080'] # Service name in Docker
      - targets: ['host.docker.internal:8080']